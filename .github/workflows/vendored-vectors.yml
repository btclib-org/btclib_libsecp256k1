---
name: vendored vectors

# tests/README.md pins every vendored test vector to a commit and a git
# blob SHA-1, with a documented manual procedure to re-check one. How
# many there are is that file's to say and not this one's. This runs
# that procedure weekly instead, over every pin the script can check,
# and opens or updates an issue on drift rather than fixing anything:
# refreshing a vector file is a decision the script does not get to
# make. Matches a sibling repository's own workflow of the same name,
# script included.
#
# issues: write is what the job below takes, and links.yml's own comment
# is where the opposite choice is argued: an automatic issue is the usual
# next step, and that workflow deliberately does not take it. This one
# does, on purpose: a link is noticed the next time somebody follows it,
# where a vendored vector is trusted without anybody looking at it again,
# and that is exactly what an opened issue is for

on:
  schedule:
    # Monday, hour 03: the calendar opens the week with the data a tree
    # ships and did not write, and this asks whether what upstream serves
    # is still what is vendored here. Weekly rather than monthly, which
    # is what the grid has to give: a vector file drifts when upstream
    # edits it in place, and the sample rate for that was never derived
    # from the subject.
    # The time is the organization grid -- section 10 of
    # btclib-org/.github's README.md -- and not this file's to restate.
    # Only the default branch schedules workflows, so on any other branch
    # this is reachable through the dispatch below alone
    - cron: "8 3 * * 1"
  workflow_dispatch:
  pull_request:
    # ready_for_review, so a pull request marked ready gets the run the
    # draft condition below withheld while it was a draft: the default
    # types do not include it, and without it a readied pull request
    # would wait for its next push to be checked at all.
    # closed joins them for a different reason: merging or closing a pull
    # request is neither a push nor a synchronize, so a run still held in
    # this pull request's own concurrency group survives it unless the
    # closed event's own run also lands there. The concurrency block
    # below is what decides whether that landing cancels it or only
    # queues behind it (#523) -- this trigger only supplies the event the
    # two cases are told apart by. "This pull request's
    # own group" is the part github.ref alone gets wrong: for a closed,
    # merged pull request it resolves to the base branch's ref, which
    # this workflow has no push trigger to collide with, but the weekly
    # schedule and a workflow_dispatch both also run on the default
    # branch and would resolve to that same ref. The concurrency block
    # below groups by the pull request's own number instead, immune to
    # that under any action
    types: [opened, reopened, synchronize, ready_for_review, closed]
    paths:
      # what a paths list costs is the wait one run adds to the checks a
      # pull request already has, which is how section 10 of the standard
      # weighs a paths-filtered pull_request on a calendar workflow --
      # not how many branches the list selects. Every path below is one
      # test.yml's `prose` pattern does not exempt, so a branch selected
      # here runs the wheel and suite matrix anyway, against a run of this
      # workflow that is a script and a keyserver fetch. The durations are
      # read back rather than assumed:
      #
      #     gh run list --repo btclib-org/btclib-secp256k1 \
      #       --workflow vendored-vectors.yml \
      #       --json createdAt,updatedAt,conclusion
      #
      # and the same call for test.yml is what it is compared against
      - .github/workflows/vendored-vectors.yml
      - .github/scripts/check_vendored_vectors.py
      - tests/README.md
      # the pin job below, and the two things it is about: the gitlink is
      # a path like any other on a pull request -- #106, which moved the
      # submodule to 0.8.0, has `secp256k1` among its changed files -- so
      # the run that answers whether a bump is a signed upstream release
      # is the run on the bump itself
      - secp256k1
      # the zkp-pin job's own equivalent (issue #613), same reasoning:
      # the commit README.md declares is a gitlink move on the pull
      # request that makes it
      - secp256k1-zkp
      - README.md

permissions:
  contents: read

concurrency:
  # github.event.pull_request.number, not github.ref alone, is what a
  # pull_request run of any action groups by -- the pull_request
  # trigger's own comment above has the reasoning. Neither schedule nor
  # workflow_dispatch carries pull_request context, so github.ref is
  # still what groups either, unchanged
  group: vendored-vectors-${{ github.event.pull_request.number || github.ref }}
  # true everywhere but one event -- a pull request closed by a merge --
  # for the reason links.yml gives at this key, this workflow having no
  # push trigger either: nothing runs it on the commit a merge creates
  cancel-in-progress: ${{ !(github.event.action == 'closed' && github.event.pull_request.merged) }}

jobs:
  check:
    name: Check vendored vectors against upstream
    # a draft pull request is work in progress and spends no runner
    # here either: the same condition lint.yml and test.yml carry, and
    # ready_for_review is a trigger type above for the same reason.
    # A closed pull request spends none either, merged or not -- the
    # concurrency block above is what stops a merged one's own run from
    # cancelling the run this condition already let finish (#523)
    if: ${{ !github.event.pull_request.draft && github.event.action != 'closed' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # the issue is this job's to open, close or edit, and only this
    # job's: the pin job below reports by going red, so it keeps the
    # workflow's own contents: read
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14"

      # no project dependency: the script imports only the standard
      # library and shells out to gh, which every GitHub-hosted runner
      # already has.
      # --dry-run on the pull_request trigger only: a fork's read-only
      # token could not write the issue anyway, and a same-repository
      # pull request testing a change to this script or the README
      # should not close or edit whatever tracking issue is open at the
      # time as a side effect of that test
      - name: Re-check every pin already at upstream's tip
        run: >-
          python .github/scripts/check_vendored_vectors.py
          tests/README.md
          "Vendored vectors behind upstream"
          ${{ github.event_name == 'pull_request' && '--dry-run' || '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # the network half of the submodule pin check, whose offline half is the
  # `submodule-pin` hook. That one asks whether the tag README.md names
  # resolves, in the vendored clone, to the commit this tree pins -- two
  # things already on the machine, which is what lets it gate a commit.
  # This asks the two that need somebody else to answer: whether that tag
  # is the one upstream publishes, and whether it carries the signature of
  # a libsecp256k1 maintainer. `version-check` in release.yml asks the
  # first of those before publishing and neither of them earlier, which is
  # what issue #126 is about.
  #
  # A sentinel and not a gate, for the reason every network check here is
  # one: a keyserver that is down is nothing a pull request introduced. It
  # runs on the pull request that moves the pin all the same -- the paths
  # filter above is what does that -- because that is where the answer is
  # wanted while acting on it is still free.
  #
  # It opens no issue where the job above does, and the asymmetry is in
  # the subject rather than in the effort. A vendored vector file drifts
  # because upstream edits it in place, with nothing here changing and
  # nobody looking, which is exactly what an opened issue is for. A pin
  # cannot drift: it moves only in a commit of this repository. So the
  # scheduled run here is a backstop against upstream retagging, or a
  # signature that stops verifying, and a red run says all of that.
  pin:
    name: Check the pinned libsecp256k1 release
    if: ${{ !github.event.pull_request.draft && github.event.action != 'closed' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          # shallow is enough: the one tag this needs is fetched below,
          # from upstream, which is the point of fetching it at all
          submodules: true

      # the fingerprints are libsecp256k1's own, the table in its
      # SECURITY.md, recorded here rather than read out of the vendored
      # tree at check time: a key list taken from the same place as the
      # tag is a list whoever controls that place also writes. Upstream
      # asks a human for the same separation in its own README, the step
      # that cross-references each fingerprint against a source its
      # owner controls. keys.openpgp.org because that is the keyserver
      # its SECURITY.md names in the command it gives
      - name: Import the libsecp256k1 maintainer keys
        env:
          # Pieter Wuille, Tim Ruffing, Sebastian Falbesoner
          FINGERPRINTS: >-
            133EAC179436F14A5CF1B794860FEB804E669320
            09E03F871092E40E106E902B33BC86AB80FF5516
            6A8F9C266528E25AEB1D7731C2371D91CB716EA7
        run: |
          read -ra fingerprints <<< "$FINGERPRINTS"
          for fingerprint in "${fingerprints[@]}"; do
            gpg --keyserver hkps://keys.openpgp.org --recv-keys "$fingerprint"
          done

      # `git tag -v` and not `git ls-remote`: the second answers which
      # commit a tag names today, which is what release.yml compares and
      # is a claim by whoever served the answer. The signature is on the
      # tag object, so the object has to be fetched to be verified
      - name: The pin is the signed upstream release README.md names
        run: |
          named=$(sed -n 's|.*secp256k1/releases/tag/\(v[0-9][0-9.]*\).*|\1|p' README.md | head -1)
          pinned=$(git ls-tree HEAD secp256k1 | awk '{print $3}')
          if [ -z "$named" ]; then
            echo "::error::README.md names no libsecp256k1 release"
            exit 1
          fi
          git -C secp256k1 fetch --force origin "refs/tags/$named:refs/tags/$named"
          git -C secp256k1 tag -v "$named"
          tagged=$(git -C secp256k1 rev-parse "$named^{commit}")
          echo "upstream's $named is $tagged; the submodule is pinned to $pinned"
          if [ "$tagged" != "$pinned" ]; then
            echo "::error::the submodule is pinned to $pinned, which is not $named"
            exit 1
          fi

  # The zkp pin's own external anchor (issue #613), the same shape as the
  # pin job above and a sentinel for the identical reason -- a keyserver
  # or a maintainer's own site being unreachable is nothing a pull
  # request introduced -- but answering a narrower question, argued in
  # full on the issue:
  #
  # secp256k1-zkp cuts no tags (issue #604's own premise), so there is no
  # signed release act to verify the way `git tag -v` verifies one above.
  # What is checkable is who wrote the pinned commit, which is a weaker
  # claim than "the project blessed this as a release" and is worded as
  # one below rather than borrowing the pin job's own wording.
  #
  # Neither secp256k1-zkp's own SECURITY.md nor mainline's is the right
  # fingerprint source: both list the disclosure contacts for
  # vulnerabilities in mainline (Pieter Wuille, Tim Ruffing, and -- only
  # in mainline's own, more current copy -- Sebastian Falbesoner), and
  # neither names Andrew Poelstra.
  #
  # The fingerprint below is his, and the reason it is the one recorded
  # is narrower than "he is the project's dominant contributor" -- he is
  # not: `git -C secp256k1-zkp log --format='%an' | sort | uniq -c | sort
  # -rn` puts him seventh by author count, at roughly 4% of the full
  # history, with Wuille, Ruffing and Nick together holding around two
  # thirds; the committer distribution (mostly the mainline-sync flood
  # `merge-script` carries in) is the same shape. What actually justifies
  # this fingerprint is narrower and true: he personally authored *and*
  # signed the one commit this tree pins, which is sufficient reason to
  # trust his key for verifying *that* pin, and is the whole of the
  # claim -- sourced the way the pin job's own comment already asks a
  # human to source one, cross-referenced against a page only its owner
  # controls, `andrew.gpg` linked from his own GitHub profile bio
  # (`gh api users/apoelstra --jq .blog`), and checked against a second,
  # independent copy, GitHub's own key store for the same account
  # (`gh api users/apoelstra/gpg_keys`); the two agree.
  #
  # What this means for the next re-pin: FINGERPRINT below is not a
  # standing maintainer roster, only a record of who signed the commit
  # pinned today. A re-pin authored or merge-script-signed by anyone
  # else -- likely, given the distribution above -- fails this job until
  # that pull request adds their fingerprint the same way, sourced and
  # cross-referenced independently rather than copied from either
  # SECURITY.md. That is a cost of every re-pin, stated here so it is
  # not rediscovered as a surprise red run.
  zkp-pin:
    name: Check the pinned secp256k1-zkp commit
    if: ${{ !github.event.pull_request.draft && github.event.action != 'closed' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          # shallow is enough: the one commit this needs is fetched
          # below, from upstream, which is the point of fetching it at
          # all
          submodules: true

      # Not keys.openpgp.org, which the pin job above fetches from: that
      # server publishes a key's user IDs only where its owner has
      # confirmed an address through it, and serves this one stripped of
      # them. GnuPG skips a key carrying no user ID on import and exits 0
      # having imported nothing, so a --recv-keys of this fingerprint
      # there reports success over an empty keyring. The bundle Andrew
      # Poelstra publishes carries the user IDs, and is the copy the
      # block comment above cross-references this fingerprint against.
      #
      # The source is a convenience and the assertion is the guard: this
      # step imports whatever that URL serves, and `gpg --list-keys` is
      # what establishes that the keyring now holds FINGERPRINT, which is
      # what the job trusts. It exits non-zero on a fingerprint the
      # keyring does not hold, so an import that brings nothing in fails
      # at the step that asked for the key rather than at the
      # verification step below, where an absent key reads as a bad pin
      # (issue #690)
      - name: Import the secp256k1-zkp signer's key
        env:
          # Andrew Poelstra
          FINGERPRINT: 90F9CA3C3EC97BE922DF955EC588D63CE41B97C1
          KEY_URL: https://www.wpsoftware.net/andrew/andrew.gpg
        run: |
          curl --fail --silent --show-error --location \
            --output "$RUNNER_TEMP/secp256k1-zkp-signer.gpg" "$KEY_URL"
          gpg --import "$RUNNER_TEMP/secp256k1-zkp-signer.gpg"
          if ! gpg --list-keys "$FINGERPRINT"; then
            echo "::error::$KEY_URL serves no key $FINGERPRINT"
            exit 1
          fi

      # `git verify-commit --raw` over its own exit code, because that
      # exit code answers a stricter question than this one is: GnuPG
      # fails a signature made with a key whose stated expiry has since
      # passed exactly the same way it fails a forged one, and this key
      # has already been measured to be in that state -- expired
      # 2026-07-28, and the pinned commit was signed 2026-09-05, weeks
      # *after* that date, on both the copy this key was imported
      # from and GitHub's own independent copy of it. That is Andrew
      # Poelstra's own key hygiene lapsing, not a reason to doubt he
      # wrote the commit: nothing about an expired-but-not-revoked key
      # weakens the cryptographic fact that this exact signature was
      # produced by the private half of the fingerprint below, which is
      # the claim this job exists to check. So the machine-readable
      # status codes are read directly -- VALIDSIG naming the expected
      # fingerprint is what a mathematically genuine signature always
      # prints, expired or not, and REVKEYSIG is the one status that
      # does mean stop trusting it, the owner's own act rather than a
      # calendar date
      #
      # Which status is printed instead of VALIDSIG decides what the
      # error says. NO_PUBKEY names a key id the keyring has nothing to
      # check the signature against, and past the import step's own
      # assertion that is some key other than FINGERPRINT: the re-pin
      # signed by somebody this job carries no fingerprint for, which
      # the block comment above gives as the likely shape of the next
      # one. BADSIG is the signature that does not verify against a key
      # the keyring does hold (issue #690)
      - name: The pin is a commit signed by a recognized secp256k1-zkp contributor
        env:
          FINGERPRINT: 90F9CA3C3EC97BE922DF955EC588D63CE41B97C1
        run: |
          named=$(sed -n 's|.*secp256k1-zkp/commit/\([0-9a-f]\{40\}\).*|\1|p' README.md | head -1)
          pinned=$(git ls-tree HEAD secp256k1-zkp | awk '{print $3}')
          if [ -z "$named" ]; then
            echo "::error::README.md links to no secp256k1-zkp commit"
            exit 1
          fi
          if [ "$named" != "$pinned" ]; then
            echo "::error::the submodule is pinned to $pinned, which README.md does not name"
            exit 1
          fi
          git -C secp256k1-zkp fetch --force origin "$pinned"
          raw=$(git -C secp256k1-zkp verify-commit --raw "$pinned" 2>&1) || true
          absent=$(sed -n 's|^\[GNUPG:\] NO_PUBKEY ||p' <<< "$raw" | head -1)
          if ! grep -q "^\[GNUPG:\] VALIDSIG $FINGERPRINT " <<< "$raw"; then
            if [ -n "$absent" ]; then
              echo "::error::$pinned is signed by $absent, which this job" \
                "holds no key for"
            elif grep -q "^\[GNUPG:\] BADSIG" <<< "$raw"; then
              echo "::error::$pinned carries a signature that does not verify"
            else
              echo "::error::$pinned carries no valid signature from $FINGERPRINT"
            fi
            echo "$raw"
            exit 1
          fi
          if grep -q "^\[GNUPG:\] REVKEYSIG" <<< "$raw"; then
            echo "::error::$FINGERPRINT has been revoked by its own owner"
            exit 1
          fi
          if grep -q "^\[GNUPG:\] EXPKEYSIG" <<< "$raw"; then
            echo "::warning::$FINGERPRINT has expired; the signature still" \
              "verifies and the key has not been revoked, so this passes" \
              "-- see issue #613"
          fi
          echo "$pinned is validly signed by $FINGERPRINT"
